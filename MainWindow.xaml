<Window x:Class="DisplayDeck.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DisplayDeck"
        Height="980"
        Width="1220"
        MinHeight="840"
        MinWidth="1080"
        WindowStartupLocation="CenterScreen"
        Background="#F4F7FB"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PageBackground" Color="#F4F7FB"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="CardBackgroundSoft" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="CardBorder" Color="#D8E1EC"/>

        <SolidColorBrush x:Key="TextPrimary" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#475569"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748B"/>

        <SolidColorBrush x:Key="AccentBlue" Color="#2563EB"/>
        <SolidColorBrush x:Key="AccentBlueDark" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="AccentBlueSoft" Color="#DBEAFE"/>

        <SolidColorBrush x:Key="AccentGreen" Color="#059669"/>
        <SolidColorBrush x:Key="AccentGreenSoft" Color="#DCFCE7"/>

        <SolidColorBrush x:Key="AccentAmber" Color="#D97706"/>
        <SolidColorBrush x:Key="AccentAmberSoft" Color="#FEF3C7"/>

        <SolidColorBrush x:Key="InputBackground" Color="#FFFFFF"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="18"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BigTvModeButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="132"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#1D4ED8"/>
        </Style>

        <Style x:Key="BigDeskModeButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="132"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
        </Style>

        <Style x:Key="KeyCap" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="34">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HERO -->
            <Border Grid.Row="0"
                    Background="{StaticResource CardBackground}"
                    BorderBrush="{StaticResource CardBorder}"
                    BorderThickness="1"
                    CornerRadius="28"
                    Padding="30">

                <StackPanel>
                    <TextBlock Text="DisplayDeck"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="44"
                               FontWeight="Bold"/>

                    <TextBlock Text="Switch your PC between desk monitors and TV gaming mode."
                               Foreground="{StaticResource TextSecondary}"
                               FontSize="18"
                               Margin="0,8,0,0"/>

                    <Border Background="{StaticResource AccentAmberSoft}"
                            BorderBrush="#F59E0B"
                            BorderThickness="1"
                            CornerRadius="18"
                            Padding="18"
                            Margin="0,24,0,0">

                        <StackPanel>
                            <TextBlock Text="Initial Setup Required"
                                       Foreground="#92400E"
                                       FontSize="20"
                                       FontWeight="Bold"/>

                            <TextBlock Text="Before saving your modes, turn on and connect every display you want to use, including the TV. DisplayDeck only shows active displays during setup so inactive virtual or phantom displays stay hidden."
                                       Foreground="#78350F"
                                       FontSize="15"
                                       TextWrapping="Wrap"
                                       Margin="0,7,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- BIG MODE SWITCHERS -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="22"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        x:Name="TvModeButton"
                        Style="{StaticResource BigTvModeButton}"
                        Click="TvModeButton_Click">

                    <StackPanel>
                        <TextBlock Text="TV Gaming Mode"
                                   Foreground="White"
                                   FontSize="31"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>

                        <TextBlock Text="Switch to TV + launch app"
                                   Foreground="#DBEAFE"
                                   FontSize="15"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="2"
                        x:Name="DeskModeButton"
                        Style="{StaticResource BigDeskModeButton}"
                        Click="DeskModeButton_Click">

                    <StackPanel>
                        <TextBlock Text="Desk Mode"
                                   Foreground="#0F172A"
                                   FontSize="31"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>

                        <TextBlock Text="Return to desktop monitors"
                                   Foreground="#475569"
                                   FontSize="15"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- SWITCHING DELAY NOTICE -->
            <Border Grid.Row="4"
                    Background="#EEF6FF"
                    BorderBrush="#BFDBFE"
                    BorderThickness="1"
                    CornerRadius="22"
                    Padding="18">

                <StackPanel>
                    <TextBlock Text="Display switching can take a moment"
                               Foreground="#1E3A8A"
                               FontSize="18"
                               FontWeight="Bold"/>

                    <TextBlock Text="When changing modes, your screens may flicker, go black, move around, or take a few seconds to settle while Windows applies the new monitor layout."
                               Foreground="#1E40AF"
                               FontSize="14.5"
                               TextWrapping="Wrap"
                               Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <!-- HOTKEYS -->
            <Border Grid.Row="6"
                    Background="{StaticResource CardBackground}"
                    BorderBrush="{StaticResource CardBorder}"
                    BorderThickness="1"
                    CornerRadius="26"
                    Padding="24">

                <StackPanel>
                    <TextBlock Text="Keyboard Shortcuts"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="24"
                               FontWeight="Bold"/>

                    <TextBlock Text="Use these anywhere in Windows while DisplayDeck is running."
                               Foreground="{StaticResource TextMuted}"
                               FontSize="14"
                               Margin="0,4,0,18"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Background="#F8FAFC"
                                BorderBrush="#D8E1EC"
                                BorderThickness="1"
                                CornerRadius="22"
                                Padding="20">

                            <StackPanel>
                                <TextBlock Text="Switch to TV Gaming Mode"
                                           Foreground="#1E3A8A"
                                           FontSize="18"
                                           FontWeight="Bold"/>

                                <StackPanel Orientation="Horizontal"
                                            Margin="0,14,0,0">

                                    <Border Style="{StaticResource KeyCap}">
                                        <TextBlock Text="Ctrl"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   FontSize="20"
                                                   FontWeight="Bold"/>
                                    </Border>

                                    <Border Style="{StaticResource KeyCap}">
                                        <TextBlock Text="Alt"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   FontSize="20"
                                                   FontWeight="Bold"/>
                                    </Border>

                                    <Border Style="{StaticResource KeyCap}">
                                        <TextBlock Text="G"
                                                   Foreground="{StaticResource AccentBlue}"
                                                   FontSize="20"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </StackPanel>

                                <TextBlock Text="Activates the TV profile and opens the selected launcher app."
                                           Foreground="{StaticResource TextSecondary}"
                                           FontSize="14"
                                           TextWrapping="Wrap"
                                           Margin="0,14,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2"
                                Background="#F8FAFC"
                                BorderBrush="#D8E1EC"
                                BorderThickness="1"
                                CornerRadius="22"
                                Padding="20">

                            <StackPanel>
                                <TextBlock Text="Switch to Desk Mode"
                                           Foreground="#334155"
                                           FontSize="18"
                                           FontWeight="Bold"/>

                                <StackPanel Orientation="Horizontal"
                                            Margin="0,14,0,0">

                                    <Border Style="{StaticResource KeyCap}">
                                        <TextBlock Text="Ctrl"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   FontSize="20"
                                                   FontWeight="Bold"/>
                                    </Border>

                                    <Border Style="{StaticResource KeyCap}">
                                        <TextBlock Text="Alt"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   FontSize="20"
                                                   FontWeight="Bold"/>
                                    </Border>

                                    <Border Style="{StaticResource KeyCap}">
                                        <TextBlock Text="D"
                                                   Foreground="#334155"
                                                   FontSize="20"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </StackPanel>

                                <TextBlock Text="Returns to desk displays and closes the launcher app if enabled."
                                           Foreground="{StaticResource TextSecondary}"
                                           FontSize="14"
                                           TextWrapping="Wrap"
                                           Margin="0,14,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- DETECTED DISPLAYS AS CARDS -->
            <Border Grid.Row="8"
                    Background="{StaticResource CardBackground}"
                    BorderBrush="{StaticResource CardBorder}"
                    BorderThickness="1"
                    CornerRadius="26"
                    Padding="24">

                <StackPanel>
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Left">
                            <TextBlock Text="Detected Active Displays"
                                       Foreground="{StaticResource TextPrimary}"
                                       FontSize="24"
                                       FontWeight="Bold"/>

                            <TextBlock Text="Only currently active displays are shown."
                                       Foreground="{StaticResource TextMuted}"
                                       FontSize="14"
                                       Margin="0,4,0,0"/>
                        </StackPanel>

                        <Button Content="Refresh"
                                Width="110"
                                Height="40"
                                HorizontalAlignment="Right"
                                DockPanel.Dock="Right"
                                Click="RefreshDisplaysButton_Click"/>
                    </DockPanel>

                    <ItemsControl x:Name="DisplaysDataGrid"
                                  Margin="0,18,0,0">

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="340"
                                        Background="#F8FAFC"
                                        BorderBrush="#D8E1EC"
                                        BorderThickness="1"
                                        CornerRadius="22"
                                        Padding="18"
                                        Margin="0,0,16,16">

                                    <StackPanel>
                                        <TextBlock Text="{Binding MonitorName}"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   FontSize="18"
                                                   FontWeight="Bold"
                                                   TextWrapping="Wrap"/>

                                        <TextBlock Text="{Binding DisplayName}"
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="13"
                                                   Margin="0,5,0,0"/>

                                        <Border Background="#E0F2FE"
                                                CornerRadius="8"
                                                Padding="10,5"
                                                HorizontalAlignment="Left"
                                                Margin="0,12,0,0">

                                            <TextBlock Text="{Binding ResolutionText}"
                                                       Foreground="#075985"
                                                       FontSize="13"
                                                       FontWeight="SemiBold"/>
                                        </Border>

                                        <StackPanel Orientation="Horizontal"
                                                    Margin="0,14,0,0">

                                            <Border Background="{StaticResource AccentGreenSoft}"
                                                    CornerRadius="8"
                                                    Padding="10,5"
                                                    Margin="0,0,8,0">

                                                <TextBlock Text="{Binding ActiveText}"
                                                           Foreground="{StaticResource AccentGreen}"
                                                           FontSize="12"
                                                           FontWeight="Bold"/>
                                            </Border>

                                            <Border Background="#E2E8F0"
                                                    CornerRadius="8"
                                                    Padding="10,5">

                                                <TextBlock Text="{Binding PrimaryText, StringFormat=Primary: {0}}"
                                                           Foreground="#334155"
                                                           FontSize="12"
                                                           FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>

                                        <TextBlock Text="{Binding PositionText, StringFormat=Position: {0}}"
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="12"
                                                   Margin="0,12,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- MODE BUILDER AS CARDS -->
            <Border Grid.Row="10"
                    Background="{StaticResource CardBackground}"
                    BorderBrush="{StaticResource CardBorder}"
                    BorderThickness="1"
                    CornerRadius="26"
                    Padding="24">

                <StackPanel>
                    <TextBlock Text="Mode Builder"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="24"
                               FontWeight="Bold"/>

                    <TextBlock Text="Choose which displays are enabled and which one is primary for each mode."
                               Foreground="{StaticResource TextMuted}"
                               FontSize="14"
                               Margin="0,4,0,0"/>

                    <Border Background="#F8FAFC"
                            BorderBrush="#D8E1EC"
                            BorderThickness="1"
                            CornerRadius="18"
                            Padding="16"
                            Margin="0,18,0,18">

                        <TextBlock Text="Tip: Set Desk Mode with your main and secondary monitors on, and TV Gaming Mode with only the TV on."
                                   Foreground="{StaticResource TextSecondary}"
                                   FontSize="14"
                                   TextWrapping="Wrap"/>
                    </Border>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Background="#F8FAFC"
                                BorderBrush="#CBD5E1"
                                BorderThickness="1"
                                CornerRadius="24"
                                Padding="20">

                            <StackPanel>
                                <TextBlock Text="Desk Mode Setup"
                                           Foreground="{StaticResource TextPrimary}"
                                           FontSize="21"
                                           FontWeight="Bold"/>

                                <TextBlock Text="Select the displays used at your desk."
                                           Foreground="{StaticResource TextMuted}"
                                           FontSize="13"
                                           Margin="0,4,0,16"/>

                                <ItemsControl x:Name="ModeBuilderDataGrid">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#FFFFFF"
                                                    BorderBrush="#E2E8F0"
                                                    BorderThickness="1"
                                                    CornerRadius="18"
                                                    Padding="14"
                                                    Margin="0,0,0,12">

                                                <StackPanel>
                                                    <TextBlock Text="{Binding MonitorName}"
                                                               Foreground="{StaticResource TextPrimary}"
                                                               FontSize="16"
                                                               FontWeight="Bold"
                                                               TextWrapping="Wrap"/>

                                                    <TextBlock Text="{Binding DisplayName}"
                                                               Foreground="{StaticResource TextMuted}"
                                                               FontSize="12"
                                                               Margin="0,3,0,0"/>

                                                    <TextBlock Text="{Binding ResolutionText}"
                                                               Foreground="{StaticResource TextSecondary}"
                                                               FontSize="12"
                                                               Margin="0,3,0,10"/>

                                                    <StackPanel Orientation="Horizontal">
                                                        <CheckBox Content="Enabled"
                                                                  IsChecked="{Binding DeskEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                  Margin="0,0,18,0"/>

                                                        <CheckBox Content="Primary"
                                                                  IsChecked="{Binding DeskPrimary, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2"
                                Background="{StaticResource AccentBlueSoft}"
                                BorderBrush="#93C5FD"
                                BorderThickness="1"
                                CornerRadius="24"
                                Padding="20">

                            <StackPanel>
                                <TextBlock Text="TV Gaming Mode Setup"
                                           Foreground="#1E3A8A"
                                           FontSize="21"
                                           FontWeight="Bold"/>

                                <TextBlock Text="Select the display used for TV gaming."
                                           Foreground="#1E40AF"
                                           FontSize="13"
                                           Margin="0,4,0,16"/>

                                <ItemsControl ItemsSource="{Binding ItemsSource, ElementName=ModeBuilderDataGrid}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#FFFFFF"
                                                    BorderBrush="#BFDBFE"
                                                    BorderThickness="1"
                                                    CornerRadius="18"
                                                    Padding="14"
                                                    Margin="0,0,0,12">

                                                <StackPanel>
                                                    <TextBlock Text="{Binding MonitorName}"
                                                               Foreground="{StaticResource TextPrimary}"
                                                               FontSize="16"
                                                               FontWeight="Bold"
                                                               TextWrapping="Wrap"/>

                                                    <TextBlock Text="{Binding DisplayName}"
                                                               Foreground="{StaticResource TextMuted}"
                                                               FontSize="12"
                                                               Margin="0,3,0,0"/>

                                                    <TextBlock Text="{Binding ResolutionText}"
                                                               Foreground="{StaticResource TextSecondary}"
                                                               FontSize="12"
                                                               Margin="0,3,0,10"/>

                                                    <StackPanel Orientation="Horizontal">
                                                        <CheckBox Content="Enabled"
                                                                  IsChecked="{Binding TvEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                  Margin="0,0,18,0"/>

                                                        <CheckBox Content="Primary"
                                                                  IsChecked="{Binding TvPrimary, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- SETTINGS -->
            <Border Grid.Row="12"
                    Background="{StaticResource CardBackground}"
                    BorderBrush="{StaticResource CardBorder}"
                    BorderThickness="1"
                    CornerRadius="26"
                    Padding="24">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="18"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="14"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.ColumnSpan="3">
                        <TextBlock Text="Launcher Settings"
                                   Foreground="{StaticResource TextPrimary}"
                                   FontSize="24"
                                   FontWeight="Bold"/>

                        <TextBlock Text="Choose an optional app to open in TV Gaming Mode and close in Desk Mode."
                                   Foreground="{StaticResource TextMuted}"
                                   FontSize="14"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2"
                               Grid.Column="0"
                               Text="Launcher app"
                               Foreground="{StaticResource TextSecondary}"
                               VerticalAlignment="Center"/>

                    <TextBox Grid.Row="2"
                             Grid.Column="1"
                             x:Name="LauncherPathBox"/>

                    <Button Grid.Row="2"
                            Grid.Column="2"
                            Content="Browse"
                            Margin="10,0,0,0"
                            Click="BrowseLauncher_Click"/>

                    <TextBlock Grid.Row="4"
                               Grid.Column="0"
                               Text="Launcher process"
                               Foreground="{StaticResource TextSecondary}"
                               VerticalAlignment="Center"/>

                    <TextBox Grid.Row="4"
                             Grid.Column="1"
                             Grid.ColumnSpan="2"
                             x:Name="LauncherProcessNameBox"/>

                    <StackPanel Grid.Row="6"
                                Grid.Column="1"
                                Grid.ColumnSpan="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">

                        <CheckBox x:Name="LaunchAppCheckBox"
                                  Content="Launch app on TV Mode"
                                  Margin="0,0,20,0"/>

                        <CheckBox x:Name="CloseAppCheckBox"
                                  Content="Close app on Desk Mode"
                                  Margin="0,0,20,0"/>

                        <CheckBox x:Name="StartWithWindowsCheckBox"
                                  Content="Start with Windows"
                                  Margin="0,0,20,0"/>

                        <Button Content="Save Settings"
                                Width="150"
                                Height="42"
                                Background="#2563EB"
                                BorderBrush="#1D4ED8"
                                Foreground="White"
                                Click="SaveSettingsButton_Click"/>
                    </StackPanel>

                    <TextBlock Grid.Row="8"
                               Grid.Column="1"
                               Grid.ColumnSpan="2"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="13"
                               TextWrapping="Wrap"
                               Text="When Start with Windows is enabled, DisplayDeck will open automatically after login and remain available in the tray."/>
                </Grid>
            </Border>

            <!-- STATUS -->
            <Border Grid.Row="14"
                    Background="{StaticResource CardBackground}"
                    BorderBrush="{StaticResource CardBorder}"
                    BorderThickness="1"
                    CornerRadius="22"
                    Padding="20">

                <StackPanel>
                    <TextBlock Text="Status"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="20"
                               FontWeight="Bold"/>

                    <TextBlock x:Name="StatusTextBlock"
                               Text="Ready."
                               Foreground="{StaticResource TextSecondary}"
                               FontSize="14"
                               TextWrapping="Wrap"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Window>